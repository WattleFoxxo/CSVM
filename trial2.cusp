using Home;
using Home.Letterbox;

house: ArrayProgram
{


struct main()
{
    string file = "test.txt"; // setup the test.txt file for public access

     int line = 0; // setup the line number for the file

    string lines = Letterbox.read(file); // read all the lines in the file

    for (line > lines) // for each line in the file
    {
        lineh = line +1 // increment the line number

        print(line + lines); // print the line number and the line
    }
}
}